doctype html
html(lang="ru")
head
    title #{title}
    meta(charset='UTF-8')
    link(rel='stylesheet', href='/css/styles.css')
body
    div.content
        form&attributes({'method': 'post', 'action': '/users'})
            input.item&attributes({'type': 'text', 'class': 'user-input', 'name': 'name', 'placeholder': 'Введите имя пользователя'})
            button.item&attributes({'type': 'submit'}) Проверить